# Отчёт: Этап 5 — Cleanup package.json

**Дата:** 2026-07-24  
**Статус:** выполнен

## Итог

Из `client/package.json` удалены:

**dependencies:**  
`@mantine/core`, `@mantine/dates`, `@mantine/form`, `@mantine/hooks`, `@mantine/modals`, `@mantine/notifications`, `@mantine/schedule`, `@mantine/tiptap`

**devDependencies:**  
`eslint-config-mantine`, `mantine-form-zod-resolver`, `postcss-preset-mantine`

Добавлены ранее: `antd`, `@ant-design/icons`.

## Прочее

- `App.tsx` без `MantineProvider`
- `main.tsx` без CSS Mantine
- Theme через `data-theme` + Ant `ConfigProvider`
- Legacy Mantine table stacks удалены; публичный API — `@/ui/data-table` / `@/components/table` DataTable
- ESLint: запрет `@mantine/*`
- `postcss.config.cjs` очищен от mantine preset
- CSS-модули IncCom/explorer очищены от `--mantine-*` vars
- `tsc --noEmit` — OK

## Критерий

```bash
rg "@mantine/" client/package.json   # пусто
rg "from '@mantine" client/src       # пусто (кроме комментариев)
npm run build
```
