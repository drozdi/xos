# Отчёт: Этап 1 — Инфраструктура ядра

**Дата:** 2026-07-24  
**Статус:** выполнен (частично по UI-формам вне Login — остальное в этапе 3)

## Итог

- Toasts: `@/ui/toast` (antd `notification`) вместо `@mantine/notifications` во всех call-sites.
- Confirm: `Modal.confirm` в `confirmAction` и `confirmNegativeBalance`.
- Login: Ant Design `Form` / `Input` / `Card` (без `@mantine/form` и `mantine-form-zod-resolver` на этом экране).
- Dates: `DateTimeField` на `antd` `DatePicker`; `DatesSettingsProvider` без `@mantine/dates`.
- Из `App.tsx` убраны `ModalsProvider` и `Notifications`.
- Из `main.tsx` убраны CSS dates/notifications (оставлены core/schedule/tiptap до этапов 4–5).

## Созданные

- `client/src/ui/toast.ts`

## Изменённые (ключевые)

- `client/src/core/confirm/confirmAction.ts`
- `client/src/core/auth/LoginScreen.tsx`
- `client/src/core/dates/DateTimeField.tsx`
- `client/src/core/dates/DatesSettingsProvider.tsx`
- `client/src/App.tsx`
- `client/src/main.tsx`
- `client/src/features/inccom/shared/notification/notification.ts`
- `client/src/features/inccom/shared/lib/negative-balance.ts`
- `client/src/features/inccom/app/providers/mantine/index.tsx` (no-op)
- Все файлы с `notifications.show` → импорт `@/ui/toast`
- Тесты: `authFlow.test.ts`, `useAppManager.test.ts` (mock `@/ui/toast`)

## Ещё на Mantine (следующий этап)

- `@mantine/core` UI по приложениям
- `@mantine/form` в остальных формах
- Theme ещё синхронизируется через Mantine color scheme
