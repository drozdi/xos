# Отчёт: E2E smoke после Mantine → Ant Design

**Дата:** 2026-07-24  
**Статус:** done

## Результат прогона

```bash
npx playwright test e2e/antd-smoke.spec.ts e2e/smoke.spec.ts e2e/theme-switch.spec.ts --reporter=list
# 8 passed, 1 skipped (E2E_INTEGRATION)
```

## Покрытие (mocked API)

| Сценарий | Статус |
|----------|--------|
| Login Ant Form | OK |
| Desktop shell после mock auth | OK |
| Main → Пользователи (`.ant-table`) | OK |
| Device → Устройства (`.ant-table`) | OK |
| IncCom `/inccom/auth/sign-in` | OK |
| SchoolTask → Расписание | OK |
| Theme → `data-theme` | OK |
| Integration login→app | skipped (нужен `E2E_INTEGRATION=true`) |

## Правки под UI

- Start Menu: разворот группы + `getByRole('button', { exact: true })`
- Theme: валидный JWT, mock account/refresh; `aria-label="Тема"`
- Mock Users: `ou`, `tutor`; Device filter: `subheader` + `value`
