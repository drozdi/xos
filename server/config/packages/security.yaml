security:
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'auto'

    providers:
        # XOS Desktop: вход по login
        login_user_provider:
            id: App\Security\UserProvider\LoginUserProvider
        # Standalone apps (IncCom): вход по email
        email_user_provider:
            id: App\Security\UserProvider\EmailUserProvider

    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false

        # Основной вход XOS (login + password)
        login:
            pattern: ^/api/login$
            stateless: true
            provider: login_user_provider
            user_checker: App\Security\UserChecker
            json_login:
                check_path: /api/login
                username_path: username
                password_path: password
                success_handler: App\Security\LoginSuccessHandler
                failure_handler: lexik_jwt_authentication.handler.authentication_failure

        # Вход standalone-приложений (email + password)
        app_login:
            pattern: ^/api/auth/login$
            stateless: true
            provider: email_user_provider
            user_checker: App\Security\UserChecker
            json_login:
                check_path: /api/auth/login
                username_path: username
                password_path: password
                success_handler: App\Security\LoginSuccessHandler
                failure_handler: lexik_jwt_authentication.handler.authentication_failure

        refresh:
            pattern: ^/api/token/refresh
            stateless: true
            provider: login_user_provider
            user_checker: App\Security\UserChecker
            refresh_jwt:
                check_path: /api/token/refresh

        api:
            pattern: ^/api
            stateless: true
            provider: login_user_provider
            user_checker: App\Security\UserChecker
            jwt: ~
            logout:
                path: /api/logout

        uploads:
            pattern: ^/uploads
            stateless: true
            provider: login_user_provider
            user_checker: App\Security\UserChecker
            jwt: ~

    access_control:
        - { path: ^/api/login$, roles: PUBLIC_ACCESS, methods: [POST] }
        - { path: ^/api/auth/login$, roles: PUBLIC_ACCESS, methods: [POST, GET] }
        - { path: ^/api/auth/register$, roles: PUBLIC_ACCESS, methods: [POST] }
        - { path: ^/api/auth/logout$, roles: PUBLIC_ACCESS, methods: [POST] }
        - { path: ^/api/token/refresh$, roles: PUBLIC_ACCESS, methods: [POST] }
        - { path: ^/api/health, roles: PUBLIC_ACCESS, methods: [GET] }
        - { path: ^/uploads/, roles: IS_AUTHENTICATED_FULLY, methods: [GET] }
        - { path: ^/api, roles: IS_AUTHENTICATED_FULLY }

    role_hierarchy:
        ROLE_ADMIN: ROLE_USER

when@test:
    security:
        password_hashers:
            Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                algorithm: auto
                cost: 4
                time_cost: 3
                memory_cost: 10
