VITE_API_URL=http://localhost:8000

# Use server-side settings API (CompositeAdapter: local + API sync)
# When true: settings are preloaded on login and synced via /api/settings
VITE_USE_API_SETTINGS=false
