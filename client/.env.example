VITE_API_URL=http://localhost:8000

# Server settings sync (apps, window geometry, start menu) via /api/settings
# Required for cross-browser / reopen restore. Set false only for local-only guest demos.
VITE_USE_API_SETTINGS=true
